/**
 * Theatre.js Studio Stub
 *
 * Empty module that replaces @theatre/studio in production builds.
 * This ensures the Studio UI is not bundled in production.
 */

// Export a minimal stub that matches the expected interface
export default {
	initialize: () => {},
	extend: () => {},
	createContentOfSaveFile: () => ({}),
};

export const extension = {};
